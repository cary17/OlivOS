# ============================================
# OlivOS Docker 镜像依赖配置
# ============================================
# 注意：优先使用 pyproject.toml，此文件仅作为备用
# ============================================

# 核心依赖 (dependencies)
Flask
Werkzeug==2.2.2
gevent
Pillow==9.3.0
psutil
pystray
requests
requests-toolbelt
rsa
websocket-client
websockets
aiohttp
qrcode
brotli
pyjson5
pywebview>=4.0.2
filetype
grpcio
grpcio-tools
protobuf

# 插件生态依赖 (extend) - 所有版本都需要
lxml
pyyaml
openpyxl
APScheduler==3.10.1
js2py
certifi
httpx
prompt-toolkit
regex
rich

# 开发工具依赖 (dev) - 仅开发版本需要
# 以下依赖在生产环境（full/core）不会安装
# pytest
# black
# flake8
# ruff
